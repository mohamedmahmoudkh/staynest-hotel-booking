const API='../backend/'; document.addEventListener('DOMContentLoaded',async()=>{const box=document.getElementById('bookingList'); const r=await fetch(API+'bookings/my-bookings.php',{credentials:'include'}); const data=await r.json().catch(()=>[]); box.innerHTML=Array.isArray(data)?data.map(b=>`<div class="card"><p>Booking #${b.id}</p><p>${b.check_in} to ${b.check_out}</p><p>Status: ${b.status}</p></div>`).join(''):'';});