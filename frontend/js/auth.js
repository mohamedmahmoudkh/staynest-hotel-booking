const API='../backend/'; const message=document.getElementById('message'); const register=document.getElementById('registerForm'); const login=document.getElementById('loginForm'); if(register) register.addEventListener('submit',async e=>{e.preventDefault(); const body={name:name.value,email:email.value,password:password.value,phone:phone.value}; const r=await fetch(API+'auth/register.php',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(body)}); message.textContent=await r.text();}); if(login) login.addEventListener('submit',async e=>{e.preventDefault(); const body={email:email.value,password:password.value}; const r=await fetch(API+'auth/login.php',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(body),credentials:'include'}); message.textContent=await r.text();});