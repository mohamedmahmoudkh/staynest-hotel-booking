const API='../backend/'; document.addEventListener('DOMContentLoaded',async()=>{const list=document.getElementById('hotelList'); if(list){const r=await fetch(API+'hotels/get-hotels.php'); const data=await r.json().catch(()=>[]); list.innerHTML=data.map(h=>`<article class="card"><h3>${h.name}</h3><p>${h.location}</p><p>${h.description||''}</p><a href="hotel-details.html?id=${h.id}">View Details</a></article>`).join('');}});