const API='../backend/'; const form=document.getElementById('bookingForm'); if(form) form.addEventListener('submit',async e=>{e.preventDefault(); const body={room_id:roomId.value,check_in:checkIn.value,check_out:checkOut.value,guests:guests.value}; const r=await fetch(API+'bookings/create-booking.php',{method:'POST',headers:{'Content-Type':'application/json'},credentials:'include',body:JSON.stringify(body)}); document.getElementById('message').textContent=await r.text();});